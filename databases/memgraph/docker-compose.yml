services:
  memgraph:
    image: memgraph/memgraph:latest
    container_name: graph-benchmark-memgraph
    restart: unless-stopped

    ports:
      - "7688:7687"
      - "3000:3000"

    volumes:
      - memgraph_data:/var/lib/memgraph

volumes:
  memgraph_data: